import { FinishedTextPipe } from './finished-text.pipe';

describe('FinishedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FinishedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
